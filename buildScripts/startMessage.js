/* ES5 Style Implementation
var chalk = require('chalk');*/

/*
    ES6 Style Implementation
    This would now be transpiled by Babel
    Make sure to run 'babel-node' instead of just 'node' for running this script
*/

import chalk from 'chalk';

console.log(chalk.green('Starting application in dev mode !!'));
